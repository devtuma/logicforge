# Phase 2: Function Block Diagram Tasks

- [x] Build math logic for AST Node width/height allocation (`FbdDiagram.tsx`).
- [x] Implement SVG shapes for AND, OR, XOR, NOT and terminals (`FbdDiagram.tsx`).
- [x] Build the reactive layout recursively routing wires from block to block.
- [x] Update `ProjectEditor` UI to allow toggling between `Ladder` and `FBD` Modes.
- [x] Manual test and render validations.
